#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

#include "libImages.h"

void main(void)
{
	IMAGE img;
	int Compteur = 0;
	int i, j;

	for (i = 0; i < img.Nblig*img.Nbcol; i++)
		if (img.data[i] = 1)
			Compteur += 1;
	printf("le nombre de pixel de l'objet est : ", Compteur);
  
